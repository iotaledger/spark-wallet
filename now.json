{
    "version": 2,
    "name": "spark-wallet",
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static-build",
            "config": { "distDir": ".build" }
        }
    ]
}
